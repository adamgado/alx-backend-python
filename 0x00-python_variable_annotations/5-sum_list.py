#!/usr/bin/env python3
"""function takes list of floats argument and returns their sum as a float."""
from typing import List


def sum_list(input_list: List[float]) -> float:
    """return sum of floats in a list"""
    return sum(input_list)
